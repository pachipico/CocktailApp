import React from 'react';
import styled from 'styled-components';

const Container = styled.SafeAreaView`
  flex: 1;
`;

const Home = () => {
  return (
    <Container>

    </Container>
  );
};

export default Home;
